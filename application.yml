server:
     port: 9000

spring:
     datasource:
        driverClassName: com.mysql.jdbc.Driver
        username: root
        password: admin
        url: jdbc:mysql://localhost:3306/sitebuilder?characterEncoding=UTF-8
        properties:
          hibernate.show_sql: true
     data:
          mongodb:
               host: localhost
               port: 27017
               database: test-restful

entitymanager:
     packagesToScan: com.spring.restful.lab.model